'use strict';

module.exports = function(Post) {
  // Post.greet = function(msg, cbl) {
  //   cbl(null, 'Greetings...' + msg);
  // };

  // Post.remoteMethod('greet', {
  //   accepts: {arg: 'msg', type: 'string'},
  //   returns: {arg: 'greeting', type: 'string'},
  // });
};
